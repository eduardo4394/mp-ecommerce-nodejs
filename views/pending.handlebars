<div>
    <h1>Pago Pendiente! ⏳</h1>

    <h4>Tipo de Pago: {{payment_type}}</h4>
    <h5>Referencia: {{external_reference}} </h5>
    <h5>ID: {{payment_id}} </h5>
    <button
      type="submit"
      class="mercadopago-button"
      onclick="location.href='/'"
   >Volver al Home</button>
</div>